// Placeholder migration runner. Real schema (documents/chunks/entities/relations/jobs)
// is implemented in ZHA-56.
import { DB_VERSION } from "./index";

console.log(`[db:migrate] placeholder — schema migration lands in ZHA-56 (db version ${DB_VERSION})`);
